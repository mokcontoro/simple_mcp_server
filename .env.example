# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Optional: Disable OAuth for local development
# ENABLE_OAUTH=false

# Optional: Override cloud service URL (default: https://app.robotmcp.ai)
# ROBOTMCP_CLOUD_URL=https://app.robotmcp.ai

# Logging: Logs are automatically sent to Supabase when connected
# No additional configuration needed - uses existing Supabase credentials
